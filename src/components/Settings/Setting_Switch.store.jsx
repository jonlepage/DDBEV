import { store } from '@risingstack/react-easy-state';

export const DATA_Setting_Switch = store({
	NAME: 'DATA_Setting_Switch',
});

export const Store_Setting_Switch = store({
	_currentSelect: 0,
	data: [],
});
